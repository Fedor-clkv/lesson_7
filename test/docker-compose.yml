version: "3.1"

services: 
  building_apps: 
    build:
      context: ./build
      dockerfile: Dockerfile
    volumes: 
      - ./vlm:/opt/vlm:rw
  
  my_app:
    build:
      context: ./app
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8080:46850
    volumes:
      - ./vlm:/opt/vlm:rw
    depends_on:
      - building_apps
  